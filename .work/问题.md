window 问题


macos:


应用通用问题：
2. 同步管理下同步操作是推送已提交的内容到远程仓库上，如果推送需要账号密码，弹出账号密码输入框[待验证]
11. 应用标题栏克隆git项目时，弹出对话框需要新增私库按钮时，新增账号密码表单项（默认不显示，点私库按钮时显示），用于私有仓库的克隆[待验证]
12. 添加Vercel AI SDK 最新稳定版本，支持ai面板上的能力；在应用设置中新增ai模型选择项，用户可以根据需要选择不同的ai模型[待验证]

ai服务本地分类是默认选择ollama;api分类是模型选择Custom (OpenAI Compatible)；


缺失能力：
1.ai能力
2.markdown导入导出能力
6.同步文件到其他远程地址能力
            
12 种图床：GitHub、阿里云 OSS、腾讯云 COS、七牛云、Cloudflare R2 等。选配好之后，插入图片自动上传、自动生成链接，省去手动传图的步骤。