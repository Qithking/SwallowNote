window 问题


macos:


应用通用问题：
2. 同步管理下同步操作是推送已提交的内容到远程仓库上，如果推送需要账号密码，弹出账号密码输入框[待验证]
11. 应用标题栏克隆git项目时，弹出对话框需要新增私库按钮时，新增账号密码表单项（默认不显示，点私库按钮时显示），用于私有仓库的克隆[待验证]


缺失能力：
1.ai能力
2.markdown导入导出能力
5.blocknote编辑器ctrl+v粘贴快速上传文件能力[待验证]
6.同步文件到其他远程地址能力

应用设置：
同步：
    文件上传路径：默认文件所在目录下，自定义路径存储格式：$folder/images.(支持变量：$folder（当前所在目录）、$rootPath（当前所在目录的根目录路径）、$userRootPath（用户根目录路径）） [待验证] 
            
12 种图床：GitHub、阿里云 OSS、腾讯云 COS、七牛云、Cloudflare R2 等。选配好之后，插入图片自动上传、自动生成链接，省去手动传图的步骤。