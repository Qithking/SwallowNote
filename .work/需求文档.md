原始需求：
    开发一款性能较好的 markdown 软件，要能有 vscode那样可以极速加载打开目录下文件信息的能力，还要能打开较大的 markdown 文件。具体要求如下：
    1.开发应用跨平台
    2.能够极速vscode那样可以极速加载打开目录下文件信息的能力，可以在树组件下流畅操作目录文件
    3.增加 git 能力。打开的目录如果没有 git，有相应的按钮可以初始化 git，从而实现 git 管理；如果有 git，则使用现有的 git 进行管理。应用可以设置 git 是否自动同步，支持自定义同步频率或是触发条件，例如保存时在提交，提交描述用提交详细时间作为提交内容。支持 git差异内容比较
    4.应用界面与 vscode 类似，左边是属性文件夹信息，右边是文件内容，支持 tab多个文件同时打开,较多文件时支持类似 vscode 快捷键快速搜索
    5.支持类似 vscode 内容过滤搜索
    6.支持多语言，初始支持中文和英文
    7.支持从剪切板快速复制文件到文件内容中，文件通过自定义规则（自定义路径及命名），自动保存到指定路径下，文件中自动生成对应的相对链接；支持自定义文件同步方式，例如 cdn 等，在文件内容中使用网络地址，文件中特别是图片文件，需要支持无损压缩
    8.支持设置多种同步方式，git/webdav/s3/ftp等
    9.除了从本地文件夹中打开外，支持从上面同步方式中打开，默认同步方式是原打开方式，支持切换，例如从 git参考打开的文档，修改后支持同步更新到其他地址例如 webdav
    10.支持工作区模式
    11.支持 ai 设置，支持本地模型和 api 模型。
    12.支持新建文件使用 ai 后通过 url快速获取页面内容并总结生成对应的 markdown 内容，并写入文件
    13.应用要求技术栈：tauri2.x+**Svelte 5**



问题：
1.应用无法打开文件夹
2.应用主界面存在滚动条
3.侧边栏文件夹图标git 图标无法点击；搜索图标和设置图标点击打开样式不正常

1.应用不要任何边框，应用高度自适应，不要出现整个页面滚动的情况
2.编辑器右边文件内容区域对系统主题，切换背景颜色
3.应用不要标题栏，页面内容置顶

去除系统标题栏，在页面顶部新增一个自定义标题栏，标题栏左边是类似 vscode 的下拉菜单，右边是自定义的最小化，最大化，关闭按钮

自定义标题栏要支持拖动应用，应用要支持圆角
添加技术栈：Tailwind CSS v4 + CSS variables+Lucide+shadcn-svelte，并应用到现在的项目中


当编辑文件是markdown 文件，使用https://github.com/Milkdown/milkdown预览文件，其他文件不要预览

1.markdown 文件没有使用milkdown预览
2.点击文件时没有打开多个 tab
3.当右边内容区域是预览模式时，点击 md 文件没有及时响应
4.当前主题是浅色，应用树组件区域和内容 tab 区域背景色和字体色不对

1.去除应用圆角设置
2.文件树组件数据项之间间距适当在增加一些
3.点击不同文件时没有打开多个 tab
4.应用左边侧边栏宽度在小一些，对于图标也在缩小一些

1.打开文件 tab 无法关闭，关闭图标会闪动
2.Activity Bar 宽度 修改为 42px,按钮图标修改为 20px

1.tab溢出时向左向右图标应该固定显示，左边没有tab时禁用向左图标，右边没有tab时禁用右左图标
2.tab溢出时更多图标点击时下拉显示内容层级不够被编辑区域遮挡没有正常显示

1.tab 左右及更多图标只有溢出时才显示，没有溢出时不显示
2.更多图标点击时显示的下拉，要使用shadcn-svelte下Dropdown Menu组件
3.请记住前端 UI优先使用shadcn-svelte相关组件，没有的情况在定制

1.标题栏就分为左右 2 个结构，左边是菜单（自适应大小），右边是自定义的最小化，最大化，关闭按钮。现在菜单字体太小；菜单栏要能支持拖拽移动应用


点击文件时，已经打开的 markdown 文件需要定位到对应的 tab 中

当切换 tab 时，需要定位到资源管理器下的文件