/**
 * Type stubs for the deep import of `simple-mind-map` that the
 * editor uses. The package only ships runtime JS; declaring the
 * default export as `any` is sufficient because every reference
 * site already treats the result as `any`.
 */
declare module 'simple-mind-map/dist/simpleMindMap.esm.js' {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const MindMap: any
  export default MindMap
}
